Install on macOS:

```sh
curl --proto '=https' --tlsv1.2 -LsSf \
  https://raw.githubusercontent.com/pkmdev-sec/orvek/main/install.sh | sh
```

Existing installations can run `orvek update`. Every binary release archive is covered by a SHA-256
checksum and minisign signature.
